server:
  port: 5050
spring:
  mvc:
    view:
      prefix: /WEB-INF/pages/
      suffix: .jsp
      

  application:
    name: donor-service

eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
